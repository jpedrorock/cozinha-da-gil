import { GuiaClient } from "./GuiaClient";

export const metadata = {
  title: "Guia do app — Cozinha da Gil",
  description: "Manual completo das funcionalidades e regras do app pra atendente, cozinha e admin.",
};

export default function GuiaPage() {
  return <GuiaClient />;
}
